# Copy this file to .env before running the production compose stack.
# Do not commit .env. It contains deployment secrets.

POSTGRES_PASSWORD=replace-with-a-long-random-password

# From Cloudflare Zero Trust -> Networks -> Tunnels -> your tunnel -> Install and run connector.
CLOUDFLARED_TOKEN=replace-with-your-cloudflare-tunnel-token
